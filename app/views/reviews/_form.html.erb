<%= simple_form_for(@review) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
      <%= f.input :date %>
      <%= f.hidden_field :build_object_id %>
  </div>

  <div class="form-actions">
    <%= f.button :submit, "Записаться" %>
  </div>
<% end %>
