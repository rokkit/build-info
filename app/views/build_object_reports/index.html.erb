<%= datagrid_form_for @grid, :method => :get, :url => build_object_reports_path, remote: true, :'data-update-target' => 'update-container' %>
<div id="update-container">
...
</div>
<%= paginate(@assets) %>
<%= datagrid_table @grid, @assets %>
<%= paginate(@assets) %>
